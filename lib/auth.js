/**
 * Check authentication token header against API to ensure request is being
 * made by a valid user account and against a project that user owns.
 * @param {Object} Request
 * @param {Object} Response
 * @callback {void}
 */
module.exports = function (req, res, next) {
    // @todo
    next();
};
